- hosts: localhost
  become: true

  vars:
    username: "tonyampomah"
    userhome: "/home/{{ username }}"

  tasks:

  - include: tasks/configure-software-manjaro.yaml
    when: ansible_facts["os_family"] == "Archlinux"
  - include: tasks/configure-user.yaml
